jobs:
  build:
    docker:
      - image: node:18.14.2
    environment: 
      repo_token: WajvOxNeVJJREYHm3BUaE3yyYwq0J8rBs
  
    steps:
      - checkout:
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Run tests
          command: npm test --coverage
          notify:
      - run:
          name: Upload coverage to Coveralls
          command: npx coveralls < coverage/lcov.info
